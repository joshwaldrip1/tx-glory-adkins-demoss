# Local design reference only — contains minors' personal data; never publish
assets/reference/

# Supabase local secrets / env
.env
.env.local

# OS / editor cruft
Thumbs.db
.DS_Store

# Node modules
node_modules/
